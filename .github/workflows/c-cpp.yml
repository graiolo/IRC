name: C/C++ CI

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: make
      working-directory: irc
      run: |
          pwd
          ls
          make
    - name: make flcean
      working-directory: irc
      run: make flcean
